# Ejemplo de .env para el proyecto — NO incluir valores reales en el repo
# Rellena esto en tu entorno local o en Secrets del proveedor/CI.

PORT=3005

# Cadena de conexión a MongoDB Atlas
# Ej: mongodb+srv://<DB_USER>:<DB_PASSWORD>@cluster0.<cluster-id>.mongodb.net/<DB_NAME>?retryWrites=true&w=majority
DB_CNN=mongodb+srv://<DB_USER>:<DB_PASSWORD>@<cluster-host>/<DB_NAME>

# Secreto para firmar JWT (poner uno fuerte en producción, no subir aquí)
JWT_SECRET=your_jwt_secret_here

# OAuth de Google (client id y client secret)
GOOGLE_ID=your_google_client_id_here
GOOGLE_SECRET=your_google_client_secret_here